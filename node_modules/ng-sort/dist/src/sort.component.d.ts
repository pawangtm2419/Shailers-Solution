import { OnInit, ElementRef } from '@angular/core';
import { SortService, SortParameter } from './sort.service';
export declare class SortComponent implements OnInit {
    private sortService;
    private elementRef;
    sortParameter: SortParameter;
    constructor(sortService: SortService, elementRef: ElementRef);
    ngOnInit(): void;
    onSortClick($event: any): void;
    readonly sortClass: string;
    private nextSort(current);
}
