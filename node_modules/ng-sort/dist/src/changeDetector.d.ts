export declare class ChangeDetector {
    private static refChanged(a, b);
    private static isArray(a);
    private static isObject(a);
    private static arrayChanged(a, b);
    private static objChanged(a, b);
    static referenceChanged(a: any, b: any): boolean;
    static propertyChanged(a: any, b: any): boolean;
}
