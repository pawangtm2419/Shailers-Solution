export interface SortParameter {
    name: string;
    asc: boolean;
    priority: number;
}
export interface Options {
    noneClass: string;
    ascendingClass: string;
    descendingClass: string;
}
export declare class SortService {
    options: Options;
    private tmp;
    private parameters;
    constructor();
    configure(options: Options): void;
    register(name: string): SortParameter;
    clear(): void;
    update(name: string, asc: boolean): void;
    sort(array: any[]): any[];
}
